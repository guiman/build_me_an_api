source 'https://rubygems.org'

# Specify your gem's dependencies in build_me_an_api.gemspec
gemspec

group :test do
  gem 'minitest', require: ['minitest/autorun']
  gem 'data_mapper'
  gem 'dm-sqlite-adapter'
end
